# DMN decision catalog

Planned decisions include responsible department, required reviewers, signatory, SLA class and dispatch channel. Inputs reference document type, sender type, amount, confidentiality, department and business context.

Decision output is workflow data. React components must not reproduce routing rules. A later dmn-js slice will add versioned editing, hit-policy validation and deterministic tests.
