# Process versioning

A designer creates a draft from a published definition. The draft is validated, tested with deterministic scenarios, reviewed by the business owner and published by an authorized publisher.

New instances resolve the latest published version. Existing instances retain the version with which they started. Migration is a separate high-risk request requiring compatibility mapping, simulation, reason, elevated permission and audit. Publication and migration never happen implicitly.
