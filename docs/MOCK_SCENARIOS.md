# Mock scenarios

The developer toolbar persists persona, locale, theme and scenario. Available scenario labels are normal operations, morning queue, process incident and restricted documents. v1.0.1 ships one deterministic dataset with an incident, urgent tasks and restricted-data affordances.

Reset removes the local database and restores the seed on the next repository read. Fixtures remain behind repository adapters and are never imported by pages.
